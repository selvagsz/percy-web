<section class="px-2 pt-3">
  <h1 class="text-xl font-semibold mb-sm">Users</h1>
  <p class="text-gray-400 w-3/4">
    These are the settings for the whole Percy organization. If you want to update settings for individual projects, select that
    project on the left. Not seeing the setting you were looking for?
    <a class="font-semibold" href="#" {{action (route-action "showSupport")}} data-test-users-show-support>Reach out</a> and let us know!
  </p>
  <div class="bg-white rounded shadow-1 p-2 mb-2">
    <div class="flex justify-between items-center mb-2">
      <h2 class="text-xl font-semibold mb-0">{{capitalize organization.name}} users ({{users.length}})</h2>
      {{#link-to "organizations.organization.users.invite" organization.slug classNames="btn btn-primary shadow-purple-lg mb-0"}}
        Invite New Users
      {{/link-to}}
    </div>
    {{organizations/users-list
      organization=organization
      organizationUsers=users
    }}
  </div>
</section>
