{{fixed-top-header currentOrganization=model}}

{{! Note: if any projects exist, the user will be redirected to the project list. }}
<div class="Card Card--extraPadded u-text-center">
  <h3>Welcome <strong>{{model.name}}</strong>!</h3>
  <p>
    {{#link-to "organizations.organization.projects.new" model.slug classNames="btn btn-primary shadow-purple-lg data-test-create-first-project"}}
      Create your first project
    {{/link-to}}
  </p>
  <p>
    After you've created a project, you can use our demo apps for experimentation.
  </p>
  {{!-- TODO: #billingadmins --}}
  {{#if (eq "admin" model.currentUserMembership.role)}}
    <p>You can also {{#link-to "organizations.organization.users.invite" model.slug class="data-test-invite-team-members"}}invite team members{{/link-to}} or {{#link-to "organizations.organization.integrations.github" model.slug class="data-test-install-github"}}install the GitHub integration{{/link-to}}.</p>
  {{/if}}
</div>

{{main-footer}}
