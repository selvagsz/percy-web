<div
  class="tech-card {{classes}} {{if isSelected 'is-selected'}}"
  {{action (optional select)}}
  data-test-tech-card
>
  <div class="flex flex-col items-center text-center">
    <figure class="svg-container">
      {{inline-svg (dasherize label)}}
    </figure>
      {{capitalize label}}
  </div>
</div>
