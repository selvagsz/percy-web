{{#if project.isEnabled}}
  <button class="btn btn-primary shadow-purple-lg mb-0" {{action "disable"}}>Archive Project</button>
{{else}}
  <button class="btn btn-primary shadow-purple-lg mb-0" {{action "enable"}}>Enable Project</button>
{{/if}}
